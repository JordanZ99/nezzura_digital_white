<style>
  #beach-white {
    position: relative;
    width: 100%;
    height: 40vh;
    background: #ffffff;
    box-sizing: border-box;
  }

  #white-titles {
    position: relative;
    width: 100%;
    height: 100%;
    box-sizing: border-box;
    will-change: transform;
  }

  #white-titles .left-title {
    position: absolute;
    top: 0;
    left: 50px;
    font-family: system-ui, -apple-system, sans-serif;
    font-weight: 448;
    font-size: clamp(48px, 5vw, 88px);
    letter-spacing: 0.05em;
    color: #080808;
    margin: 0;
  }

  #white-titles .right-title {
    position: absolute;
    top: 0;
    right: 50px;
    font-family: system-ui, -apple-system, sans-serif;
    font-weight: 448;
    font-size: clamp(48px, 5vw, 88px);
    letter-spacing: 0.05em;
    color: #080808;
    margin: 0;
  }

  #white-titles .description {
    position: absolute;
    top: 115%;
    left: 50%;
    transform: translate(-50%, -50%);
    font-family: system-ui, -apple-system, sans-serif;
    font-weight: 500;
    font-size: clamp(16px, 1vw, 22px);
    line-height: 1.6;
    text-transform: uppercase;
    text-align: center;
    color: #888888;
    margin: 0;
    width: 80%;
    max-width: 460px;
    white-space: pre-line;
  }

  @media (max-width: 600px) {
    #white-titles .left-title {
      top: 10px;
      left: 20px;
      font-size: clamp(28px, 7vw, 42px);
    }
    #white-titles .right-title {
      top: 10px;
      right: 20px;
      font-size: clamp(28px, 7vw, 42px);
    }
    #white-titles .description {
      top: 60%;
      left: 50%;
      transform: translate(-50%, -50%);
      width: 90%;
      max-width: 340px;
      font-size: clamp(12px, 3.2vw, 16px);
    }
  }
</style>

<section id="beach-white">
  <div id="white-titles">
    <p class="left-title">DIGITALIZA</p>
    <p class="right-title">TU NEGOCIO</p>
    <p class="description">78% de los negocios en México pierde clientes por no tener presencia digital profesional.</p>
  </div>
</section>
